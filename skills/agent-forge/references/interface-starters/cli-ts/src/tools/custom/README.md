# Custom tools

Bespoke tools (not provided by an MCP server) live here. Each tool is its own
`.ts` file exporting a definition matching the harness's tool API.

The skill generation step writes tool files into this directory.
