# Copy to .env for local development. In packaged builds these values come from the
# Settings UI (electron-store) instead — bundled .env files don't ship with installers.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
