// Tailwind v4 is wired through the `@tailwindcss/vite` plugin in vite.renderer.config.ts,
// so PostCSS is not strictly required. This file is kept as a stub: if you later need
// other PostCSS plugins (autoprefixer, etc.) or you switch to `@tailwindcss/postcss`,
// add them here and they'll be picked up by Vite automatically.
export default {
  plugins: {},
};
